// src/components/Header.tsx
// Old per-page header is now disabled. Pages can keep <Header /> without rendering anything.
export function Header(_props: any) {
  return null;
}
export default Header;
