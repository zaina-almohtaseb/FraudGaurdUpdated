// lib/config.ts  (DEV ONLY — okay for demo)
// Point this at your local Flask server or a public URL if you have one
export const API_BASE = "http://127.0.0.1:5000";

// Supabase (public values are fine in client)
export const SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
export const SUPABASE_ANON = "YOUR_ANON_KEY";
